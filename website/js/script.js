// user log in
let username
let password

// profile log in
function checkprofile() {

    // create element
    const container = document.createElement("div")
    

    // check
    if (username && password) {
        // html

    } else {

    }
}